
@page
@using global::Pages.Roles
@model IndexModel
@{
    ViewData["Title"] = "Roller";
}

<h1>Roles</h1>

<p>
    <a asp-page="Create" class="btn btn-primary">Create New Role</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var role in Model.Roles)
        {
            <tr>
                <td>@role.Name</td>
                <td>
                    <a asp-page="Edit" asp-route-id="@role.Id" class="btn btn-warning">Edit</a>
                    <a asp-page="Delete" asp-route-id="@role.Id" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
