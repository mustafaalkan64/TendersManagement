
@page
@using global::Pages.Roles
@model IndexModel
@{
    ViewData["Title"] = "Roller";
}

<h1>Roller</h1>

<p>
    <a asp-page="Create" class="btn btn-primary">Yeni Rol Oluştur</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Rol</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var role in Model.Roles)
        {
            <tr>
                <td>@role.Name</td>
                <td>
                    <a asp-page="Edit" asp-route-id="@role.Id" class="btn btn-warning">Düzenle</a>
                    <a asp-page="Delete" asp-route-id="@role.Id" class="btn btn-danger">Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>
